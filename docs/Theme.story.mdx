import { Meta } from '@storybook/addon-docs/blocks';
import { baseTheme } from "../src";

<Meta title="Docs/Theme" />

# Theme

The `theme` is based on the
[System UI Theme Specification](https://styled-system.com/theme-specification). You can use the base
theme by importing it from the library - or you can copy the values and adapt it fit your needs.

```typescript
import { baseTheme } from '@charlietango/ui';

export const theme = { ...baseTheme };
```

To use the theme, add it the application using a `<ThemeProvider />`

```jsx
const Example = () => (
  <ThemeProvider theme={theme}>
    <App />
  </ThemeProvider>
);
```

## Base theme [./src/theme.ts](https://github.com/charlie-tango/ui/blob/master/src/theme.ts)

<pre>
  <code>{JSON.stringify(baseTheme, null, 2)}</code>
</pre>
